title: gly-3 · Recomputación por eventos, ventana abierta IN_PROGRESS y GET /events/{id}/glycemic-response
labels: module:glycemic,machine:imac,phase:1
**Módulo:** `glucurvia-glycemic` · **Máquina:** iMac · **Fase:** 1

**Contexto:** diseño 6.3 y 7; plan 2 (listeners asíncronos tras commit). Depende de gly-2.

**Contratos que usa:** `MealEventReader` y `GlucoseSeriesReader` (stubs hasta que existan las implementaciones); implementa `GlycemicResponseReader`; escucha `ReadingsIngested`, `EventLogged` y `EventUpdated`.

**Alcance:** solo `glucurvia-glycemic` y `glucurvia-schema/.../db/migration/glycemic` (la tabla ya está en el baseline).

**Terminado cuando:**
- [ ] Listeners `@Async @TransactionalEventListener(phase = AFTER_COMMIT)`: `ReadingsIngested` recomputa las comidas cuya ventana `[t0 − 60, t0 + 240]` toca el rango; `EventLogged`/`EventUpdated` de tipo MEAL recomputan esa comida; otros tipos recomputan las comidas de su ventana (etiquetas de contexto).
- [ ] Ventana abierta: bandera `IN_PROGRESS`, se recomputa en cada pull; al cerrar (+240) cálculo final sin la bandera.
- [ ] Reconciliador `@Scheduled` que recomputa lo pendiente o fallido; `recomputeFor(mealId)` idempotente.
- [ ] `GET /events/{id}/glycemic-response` y `POST .../recompute` exactamente como el contrato (`GlycemicResponseView` con `curve` de 5 min desde −60).
- [ ] `GlycemicResponseReader` real pasa `AbstractGlycemicResponseReaderContract` (Testcontainers).
- [ ] `./mvnw -q -pl glucurvia-glycemic -am spotless:apply verify` en verde.
