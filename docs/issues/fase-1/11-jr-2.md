title: jr-2 · Correcciones, repeat_of_event_ref y eventos de contexto
labels: module:journal,machine:macbook,phase:1
**Módulo:** `glucurvia-journal` · **Máquina:** MacBook · **Fase:** 1

**Contexto:** diseño 5.2 y 5.4. Depende de jr-1.

**Contratos que usa:** `EventJournal` (propio); `NutrientEstimator` (stub o real) para re-estimar tras una corrección de ítems.

**Alcance:** solo `glucurvia-journal`.

**Terminado cuando:**
- [ ] `PATCH /events/{id}` con `meal` re-estima y guarda una nueva `estimator_version` (`…+user`); las correcciones que crean alias llaman a `rememberCorrection`.
- [ ] `repeat_of_event_ref` se resuelve buscando en la ventana deducida: un candidato → copia ítems con `macro_source` heredado; varios → error claro con la lista; ninguno → error claro.
- [ ] Validación por tipo de `attributes` (diseño 5.2) con mensajes en español; `SYMPTOM.ongoing` conservado.
- [ ] `./mvnw -q -pl glucurvia-journal -am spotless:apply verify` en verde.
