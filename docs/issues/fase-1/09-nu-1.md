title: nu-1 · Catálogo es-MX: semilla, convención de hidratos y búsqueda por trigramas
labels: module:nutrition,machine:macbook,phase:1
**Módulo:** `glucurvia-nutrition` · **Máquina:** MacBook · **Fase:** 1

**Contexto:** diseño 9.2 y 9.5. La tabla `food_references` ya existe con la restricción de `carb_convention`.

**Contratos que usa:** ninguno de core todavía (nu-2 implementa el adaptador).

**Alcance:** solo `glucurvia-nutrition` y `glucurvia-schema/.../db/migration/nutrition`. El archivo de semilla queda exento del límite de 400 líneas.

**Terminado cuando:**
- [ ] Semilla de ~150 alimentos de México (tortilla de maíz y harina, tostada horneada y frita, frijoles, arroz rojo, bolillo, concha, tamal, atole, aguas frescas, nopales, aguacate, quesos, chilaquiles, huevo, pollo, avena en hojuelas, frutas comunes, bebidas de avena y almendra, yogur…) con `per_100g` que incluya `carb_convention` (`BY_DIFFERENCE` para USDA, `MX_NOM051` para etiqueta mexicana) y `carbs_available` coherente; `portions` con pieza, taza, cucharada copeteada y rasa, puño, vaso 250/355 ml.
- [ ] Los 40 alimentos más frecuentes verificados a mano contra USDA o SMAE y anotados en `source_notes`.
- [ ] `search_text` normalizado por la aplicación (sin acentos, minúsculas) y búsqueda `pg_trgm` que resuelve "abena", "hojuelas", "tostada".
- [ ] Un test de catálogo rechaza filas sin convención y comprueba que `carbs_available ≤ carbs_total`.
- [ ] `./mvnw -q -pl glucurvia-nutrition -am spotless:apply verify` en verde.
