title: cgm-2 · LibreViewCsvAdapter (respaldo e historial)
labels: module:cgm,machine:imac,phase:1
**Módulo:** `glucurvia-cgm` · **Máquina:** iMac · **Fase:** 1

**Contexto:** diseño 4.5 (una generación, un idioma, falla ruidosa) y 4.1 (LO/HI, cadencia detectada).

**Contratos que usa:** `CgmSourceAdapter` interno del módulo (no es adaptador de core); publica `ReadingsIngested`.

**Alcance:** solo `glucurvia-cgm`. Depende de cgm-1 solo para compartir `CgmIngestionService` (si no está mergeado, crea el servicio aquí y cgm-1 lo reutiliza).

**Terminado cuando:**
- [ ] Localiza la fila de cabecera (metadatos antes) y la valida contra el diccionario del idioma de la cuenta; si no coincide, falla con un mensaje claro.
- [ ] `Record Type` 0 y 1 se ingieren como `HISTORIC` y `SCAN`; 4, 5 y 6 se ignoran; cualquier otro se rechaza con aviso y valor crudo en `warnings`.
- [ ] Formato de fecha detectado por muestreo; conversión con `assumed_timezone`; hora local inexistente o ambigua → fila rechazada con aviso (sin lógica de resolución).
- [ ] LO/HI con parseo defensivo; valores no numéricos a `warnings`; recortados con `is_clipped`.
- [ ] `detected_cadence_sec` calculado con la mediana de los intervalos.
- [ ] Corpus de dos archivos en `src/test/resources` (uno real anonimizado, uno mutado: solapamiento, LO, tipo desconocido, fecha imposible) como test de regresión.
- [ ] `./mvnw -q -pl glucurvia-cgm -am spotless:apply verify` en verde.
