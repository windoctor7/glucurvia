title: cgm-4 · GET /cgm/readings con resolución raw y 5m
labels: module:cgm,machine:imac,phase:1
**Módulo:** `glucurvia-cgm` · **Máquina:** iMac · **Fase:** 1

**Contexto:** diseño 7; el remuestreo es `Resampler` de `core` (compartido con glycemic).

**Contratos que usa:** `Resampler` y `Series` (core).

**Alcance:** solo `glucurvia-cgm`.

**Terminado cuando:**
- [ ] `resolution=raw` limitado a 31 días (error claro si se excede); `5m` para cualquier rango, con marca `observed` por punto.
- [ ] Respuesta exactamente como `ReadingsResponse` en `docs/api/openapi.yaml`.
- [ ] `./mvnw -q -pl glucurvia-cgm -am spotless:apply verify` en verde.
