title: jr-1 · Eventos, comidas e ítems: EventJournal y MealEventReader reales
labels: module:journal,machine:macbook,phase:1
**Módulo:** `glucurvia-journal` · **Máquina:** MacBook · **Fase:** 1

**Contexto:** diseño 5 y 3 (tablas `events`, `meals`, `meal_items`, ya en el baseline). journal guarda lo que recibe: la comida llega ya estimada (`NewEvent.meal`).

**Contratos que usa:** implementa `EventJournal` y `MealEventReader`; publica `EventLogged` y `EventUpdated`.

**Alcance:** solo `glucurvia-journal` y `glucurvia-schema/.../db/migration/journal`.

**Terminado cuando:**
- [ ] `log` persiste evento + comida + ítems en una transacción y publica `EventLogged` tras commit; `update` aplica `EventPatch` (hora, atributos, comida re-estimada) y publica `EventUpdated`; `delete` es lógico (`deleted_at`).
- [ ] `search` filtra por usuario, rango, tipos y texto (sin acentos ni mayúsculas), ordenado por `started_at`.
- [ ] `mealsBetween` devuelve `MealEvent` con hidratos, confianza, alimento dominante (ítem con más hidratos) y nombres de alimentos; `contextBetween` nunca incluye comidas.
- [ ] Endpoints `GET /events`, `GET/PATCH/DELETE /events/{id}`, `POST /events` exactamente como el contrato (`POST /events` con `meal` llama a `NutrientEstimator` antes de guardar).
- [ ] Ambas implementaciones pasan sus `Abstract…Contract` con Testcontainers.
- [ ] `./mvnw -q -pl glucurvia-journal -am spotless:apply verify` en verde.
