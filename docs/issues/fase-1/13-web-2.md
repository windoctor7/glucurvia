title: web-2 · Detalle de comida: curva de una ventana y cajón "¿por qué este número?"
labels: module:web,machine:macbook,phase:1
**Módulo:** `web/` · **Máquina:** MacBook · **Fase:** 1

**Contexto:** diseño 1.4 (eje relativo −1 h … +4 h, bottom sheet), 10.5 y `GlycemicResponseView` del contrato.

**Contratos que usa:** `GET /events/{id}` y `GET /events/{id}/glycemic-response`.

**Alcance:** solo `web/`.

**Terminado cuando:**
- [ ] Gráfico con Recharts de la `curve` con eje relativo a la comida, línea de basal, marca del pico y sombreado de la zona sobre `personalP90Mgdl`; puntos no observados en trazo discontinuo; `IN_PROGRESS` indicado como provisional.
- [ ] Ficha de métricas (basal, delta, tiempo al pico, iAUC 0–120, recuperación, calidad con su motivo).
- [ ] Bottom sheet por ítem con fila de catálogo, gramos con rango, supuestos y `macroSource` (`MealItemDetail`).
- [ ] Mocks con datos sintéticos para las nueve curvas del diseño; comandos de verificación en verde.
