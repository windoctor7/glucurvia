title: nu-2 · NutrientEstimator real: resolución, rangos, confianza, banda y política de aclaración
labels: module:nutrition,machine:macbook,phase:1
**Módulo:** `glucurvia-nutrition` · **Máquina:** MacBook · **Fase:** 1

**Contexto:** diseño 9.1, 9.3 y 10. Las reglas puras ya están en `core` (`NutrientMath`, `ClarificationPolicy`, `Confidence`): úsalas, no las reimplementes. Depende de nu-1.

**Contratos que usa:** implementa `NutrientEstimator`.

**Alcance:** solo `glucurvia-nutrition`. Sin LLM: la extracción llega hecha (`ExtractedMeal`).

**Terminado cuando:**
- [ ] Resolución: alias del usuario → catálogo (trigramas) → `fallbackPer100g` marcado `LLM_FALLBACK` y alimento encolado en `food_references` con `source = LLM_FALLBACK` para curación.
- [ ] Gramos desde mililitros con `density_g_ml`; recetas con `yield_g` y subítems; `portion_eaten`.
- [ ] Confianza derivada en Java de las cuatro señales (unidad explícita, alias o etiqueta, score de catálogo, anchura relativa); la del LLM solo se guarda.
- [ ] Banda de la comida con `NutrientMath.mealBand`, peor caso con `Range.sumWorstCase`, pregunta única con `ClarificationPolicy`.
- [ ] `rememberCorrection` escribe `user_food_aliases`; la siguiente estimación lo usa con `USER_ALIAS` y confianza 0,9.
- [ ] La cena del diseño 9.3 (avena, media manzana, tostada, 120 ml de bebida de avena) da ≈50 g (40–60) y no pregunta.
- [ ] Pasa `AbstractNutrientEstimatorContract`; set de 25–40 frases reales como fixtures.
- [ ] `./mvnw -q -pl glucurvia-nutrition -am spotless:apply verify` en verde.
