title: web-1 · Chat móvil-primero contra los mocks del contrato
labels: module:web,machine:macbook,phase:1
**Módulo:** `web/` · **Máquina:** MacBook · **Fase:** 1

**Contexto:** diseño 1.4 y 8.1. Ya existen el esqueleto, los tipos generados y los mocks de `/cgm/status` y `/chat/messages`.

**Contratos que usa:** `docs/api/openapi.yaml` (si falta algo, PR de contrato que cambie solo ese archivo).

**Alcance:** solo `web/`.

**Terminado cuando:**
- [ ] Pantalla de chat a 390 px: historial con burbujas, caja fija abajo con `safe-area`, envío con Enter, estado "enviando", errores legibles (sin API, 5xx).
- [ ] La respuesta muestra `createdEvents` como tarjetas (tipo, hora local, ≈HC (rango), confianza) y `clarification` como pregunta destacada.
- [ ] Cabecera con minutos desde la última lectura (`/cgm/status`) y aviso "sin datos nuevos desde hace X min" cuando supere 15.
- [ ] Mocks ampliados en `src/mocks/handlers.ts` para estos casos; `npm run gen && npm run lint && npm run format:check && npm run build && npm test` en verde.
